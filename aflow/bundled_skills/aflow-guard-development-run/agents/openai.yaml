interface:
  display_name: "Guard AFlow Run"
  short_description: "Guard runs with visual reports and upgrade audits"
  default_prompt: "Use $aflow-guard-development-run to guard this AFlow run, audit its worker upgrade strategy, and deliver compact HTML, PNG, and PDF reports in this task."
