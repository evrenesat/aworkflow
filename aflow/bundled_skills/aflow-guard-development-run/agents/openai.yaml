interface:
  display_name: "Observe AFlow Run"
  short_description: "Observe runs with bounded deterministic A4 reports"
  default_prompt: "Use $aflow-guard-development-run for one exact pinned run; stay read-only, keep healthy ticks silent, and produce the bounded PNG/JSON report only for an explicit :vr request."
