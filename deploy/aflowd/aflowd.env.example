# Install as /etc/aflowd/aflowd.env with mode 0600, owned by the service user.
# Use an opaque bearer token without whitespace or shell quoting. Do not commit
# the populated file and never put this value in a URL.
AFLOW_APP_TOKEN=replace-with-a-generated-bearer-token
