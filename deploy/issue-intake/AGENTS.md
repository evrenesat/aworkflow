# Issue-intake deployment examples

This directory contains inactive, copy-only operator material for the
one-shot issue-intake command. Do not move its workflow example under
`.github/workflows/`, add credentials, or turn placeholders into live runner
details without explicit owner authorization. The relay accepts metadata only;
issue text and provider/API credentials must stay out of Actions logs.
