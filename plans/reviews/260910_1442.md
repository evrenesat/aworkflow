# CP4 review: responsive document scrolling and mobile editing

Reviewed the immediately preceding worker's uncommitted CP4 attempt v01 against original Checkpoint 4. ACTIVE_PLAN and ORIGINAL_PLAN are `plans/in-progress/responsive-document-scroll-and-mobile-editing-20260910.md`. Used current-worktree fallback from approved `cp3 v02` (`8246325`): no CP4 checkpoint commit exists. The worker-checked heading is completion intent, not review approval; the cp05 repair filename does not change scope. Branch matches Git Tracking and the pre-handoff base is reachable.

## Finding

### P2 — Keep the wrapping control outside the editable viewport

Confidence: high. Location: `apps/aflow_app/web/src/styles.css:1008–1018` (related padding at line 1043).

Open Skills at 390×844, load a long document, and scroll inside the native textarea. The absolutely positioned 44px Wrap lines toolbar stays over its upper-right corner, but the textarea's 60px top padding scrolls away with its content. Text then renders underneath the opaque toolbar and clicks intended to position the caret hit Wrap lines instead. This affects every shared TextEditor consumer and violates CP4's usable editing and unobscured-focus contract.

Evidence: a disposable authenticated Chromium Skills journey using the real built assets measured textarea scrollTop=600, paddingTop=60px, toolbar y423.56–467.56 inside textarea y422.56–844.56. Hit-testing the overlap returned the Wrap lines span. Inspected `/tmp/cp4-review-real-skills.png` visibly shows document text cut off under that control. Existing tests assert outer textarea dimensions and scrollTop, so they miss this obstruction.

Smallest coherent fix: put the toggle in a non-overlapping label/toolbar area outside the textarea, remove compensating textarea padding, and preserve normal document flow and the existing task-space budgets. Add a browser regression exercising native scrolling with long text and verifying the control cannot cover or intercept the editable region. Do not alter draft/save ownership or stored bytes.

## Verification and disposition

- Full web suite: 297 passed. Production build passed.
- Existing Settings and Runs Chromium modules: 4 passed.
- Disposable loaded-app probes: 2 passed; inspected the Skills screenshot and confirmed the finding by geometry and hit-testing. A compact profile-column observation was not admitted as a separate material finding.
- `git diff --check` passed before bookkeeping.
- CP4 remains unapproved; CP5–6 remain unchecked. Focused non-checkpoint repair: `plans/in-progress/responsive-document-scroll-and-mobile-editing-20260910-cp05-v01.md`.
- No checkpoint approval commit or history rewrite. Publication, exact-SHA CI, and live activation remain pending normal workflow delivery. Chromium emulation is not physical-device or WebKit proof.

Material fixes required
