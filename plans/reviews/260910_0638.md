# CP4 review — material fixes required

Reviewed the immediately preceding worker's pending CP4 implementation using worktree fallback against approved CP3 `4ea9810` on `codex/aflow-dogfood-20260909`. No pending CP4 commit exists; older CP4 commits belong to preceding plans. Original plan: `plans/in-progress/live-configuration-without-snapshot-gates-20260909.md`. Scope: live session identity, cached supervision refresh and hotplug continuity. CP5 is not reviewed.

## Finding

P2, high confidence — Live harness mapping changes bypass source handover.
Location: `aflow/workflow.py:9554-9564` (comparison gated by `request.role_selectors.get("worker")` at lines 9539–9541).

The new effective-execution comparison only runs for explicit worker role overrides. Changing saved worker mappings from `codex.base` to newly added `reasonix.new` between turns bypasses transaction creation. The next call starts a new target session without the source handover required by original CP4. This is a concrete missing part of CP4's new live-configuration integration, rather than a request to redesign the established hotplug protocol.

Evidence: an isolated two-turn run using `_make_live_source`, `_live_config`, `_run_live`, `RecordingAdapter`, and `RecordingSessionDriver` changed all `worker = "codex.base"` mappings to `worker = "reasonix.new"` and appended `[harness.reasonix.profiles.new]` with `model = "new-model"` during turn 1. Captured selectors were `["codex.base", "reasonix.new"]`; the second request had `session_id=None`, no `Source worker handover:` appendix, and persisted `hotplug_history=[]`. No actual provider was used. Code only invokes cross-harness handover when a matching transaction exists, in both ordinary and retry paths.

Impact: supported live cross-harness changes discard conversational continuity and bypass the required handover before target execution.

Smallest coherent fix: reconcile the current worker target against captured source session facts for all live selection paths, and reuse existing hotplug transaction/handover machinery. Preserve completed/pending handover facts and source sessions even when the source profile disappears. Add the focused synthetic regression.

## Verification and bookkeeping

Required CP4 suite: 224 passed in 6.44s. Synthetic continuity check reproduced the gap. `git diff --check` passed. Pre-handoff base remains reachable and branch matches. No implementation or Git history was rewritten; CP4 approval is unchecked, compatible pending work retained, and CP5 untouched. Review overlay: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp05-v01.md` (original CP4 scope, no checkpoint headings). Existing overlays for earlier checkpoints were not superseded by this CP4 review. The 40 KiB manager hard guard and 16 KiB compact summary remain unchanged. No global settings, services or real workflows were modified.

Material fixes required
