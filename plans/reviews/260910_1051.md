# CP10 review — pending checkpoint prompt context

Reviewed the immediately preceding worker's dirty CP10 implementation against approved CP9 `ff7f388` on `codex/aflow-dogfood-20260909`. Worktree fallback was used: zero intervening implementation commits. Original scope is checkpoint 10 of `plans/in-progress/live-configuration-without-snapshot-gates-20260909.md`; CP11 and later implementation are excluded. Existing plan bookkeeping and the CP13 delivery amendment are preserved.

## Finding

- **P2 — Recover worker evidence independently of manager-boundary replay. Confidence: high.** `aflow/workflow.py:3655–3670`. A stopped legacy run without an active implementation scope can have a fully finalized CP7 worker result, CP6 recorded approved, and CP8 as the next unchecked heading. `_pending_finalized_resume_turn` returns `None` for a normally stopped run because it only recovers interrupted manager boundaries. The new renderer only calls `_recovered_review_target` when that boundary object exists; otherwise it returns no context. The reviewer therefore receives neither CP7's identity/artifact nor an ambiguity explanation, leaving the original wrong-checkpoint review scenario unresolved. A temporary synthetic predecessor with CP7 `snapshot_before` and CP8 `snapshot_after`, followed by an actual injected-runner `run_workflow` resume at review, captured one reviewer prompt with no `Checkpoint under review` section and no CP7 identity. Smallest fix: recover the latest relevant finalized worker metadata for scope-less review resumes independently of boundary replay, reuse the existing target validation/rendering, and explicitly describe unresolved evidence. Do not replay an already completed boundary or add an approval ledger.

## Verification and disposition

- Required CP10 suite: `uv run pytest tests/test_runtime.py tests/test_run_state.py tests/test_skill_install.py -q` — 321 passed, 43 subtests passed.
- Focused CLI seam: `uv run pytest tests/test_cli.py -q -k pending_finalized` — 1 passed, 153 deselected.
- Synthetic actual resumed prompt reproduction confirmed the finding; temporary files and fake provider only.
- `git diff --check` passed.
- CP10 remains unapproved. Compatible implementation and implementation-step markers retained; CP11 untouched. Focused non-checkpoint repair overlay: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp10-v02.md`.
- Manager 40 KiB hard guard and compact 16 KiB summary unchanged. No production code changed by review, history rewrite, global configuration/skill changes, service changes, or publication.

Material fixes required
