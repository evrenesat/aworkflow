# CP9 pending worker review — 2026-09-10

Target: immediately preceding CP9 worker worktree, against approved CP8 `c0e5220` on `codex/aflow-dogfood-20260909`. Worktree fallback: no pending CP9 commit exists; zero intervening implementation commits. The worker checked CP9, but it has not been approved. CP10 is outside this review. Original and active plan are `plans/in-progress/live-configuration-without-snapshot-gates-20260909.md`.

Scope: New Run preflight API/types, status/path list, pagination, acknowledgment state, startup-question continuation, ordinary and restart launch wiring, and frontend tests. Retain compatible approved implementation and the manager 40 KiB hard guard / compact 16 KiB summary.

## Finding

**P2 — Restore dirty startup answers when reopening a pending run. Confidence: high.**

Location: `apps/aflow_app/web/src/components/RunDashboard.tsx:1980` (line 1980).

A user starts a run, receives a dirty-worktree startup question, then reloads or reopens that pending run on the Runs page. The existing selected-run effect restores `evidence.startup_question`, but the changed render condition now excludes every `confirm_worktree_dirty` question. Its replacement panel is rendered only inside New Run (`newRunPage` at line 2200). Consequently the selected pending run shows neither its question nor any answer controls and cannot be continued or declined there.

Evidence: a temporary DOM reproduction returns an `awaiting_startup_answer` run with a synthetic `confirm_worktree_dirty` question from list/detail mocks, opens that exact run, and fails to find its message. The old render condition admitted that question and offered answers. The temporary test was removed after verification.

Smallest correction: limit suppression of the existing startup-question card to the New Run flow, preserving answer controls for the selected pending run on Runs. Keep New Run's inline list/checkbox and existing question/idempotency identity. Add a regression covering reopening and answering the exact pending question without another start request.

## Verification and disposition

- Full web suite: 289 passed, one clipboard-feedback test failed; that exact test passed on focused retry. Do not characterize the initial full run as clean.
- Production web build passed; `git diff --check` passed.
- Synthetic reopened-question regression reproduced the finding.
- Chromium smoke was not performed; no Chromium executable is on PATH. Worker recorded the limitation. Browser verification remains outstanding.
- CP9 remains unapproved; implementation retained. Focused non-checkpoint repair overlay: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp10-v01.md` (original CP9 scope despite filename). No implementation edits, history rewrite, deployment, real provider calls, global configuration/skills or service edits.

Material fixes required
