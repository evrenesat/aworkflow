# Checkpoint 3 review — 2026-09-10

Reviewed the immediately preceding worker turn-005's uncommitted checkpoint 3 changes against `ff25d251bcdbc5c4a82073fa067351a8a1c24aa8` (`cp2 v01`) using the worktree fallback: no checkpoint 3 commit exists. Branch matches the original plan; pre-handoff base is reachable. Scope: package entry point removal, standalone daemon main removal, deployment executable validation and direct tests. Plan checkbox edits are worker progress bookkeeping. Older approved checkpoints and checkpoint 4 are outside this review.

## Finding

**P2 — Remove the obsolete launcher from actual release staging. Confidence: high.**

Location: `deploy/aflowd/install.sh:245–252`; caused by removal of `aflow.daemon.main` in this checkpoint (`aflow/daemon.py`, former lines 2387–2420).

On a fresh staged release, the installer still writes `bin/aflowd` using `from aflow.daemon import main` and hashes it into the manifest. That function no longer exists. Thus real releases still ship a broken standalone executable, violating checkpoint 3's removal contract; invoking it fails with ImportError. The service continues to execute `aflow-app-server`, so this is not a claimed service outage. The updated hand-built fixtures omit aflowd, but do not represent the actual installer output.

Smallest fix: delete the obsolete `write_entrypoint` call and its manifest hash argument, and extend the existing fake-tool fresh-install test to verify generated executable and manifest contents. Preserve retained executable and integrity checks.

## Verification and disposition

233 tests and 129 subtests passed across `test_aflowd_deploy.py`, `test_aflowd_continuous_deploy.py`, `test_control_plane_resume.py`, and `test_cli.py`, using an isolated HOME. ShellCheck passed for `validate-runtime.sh`; metadata checks confirmed aflowd is absent and aflow retained; importing the daemon module confirmed main is absent. Source inspection confirmed the surviving installer import target and manifest entry. `git diff --check` passed.

Checkpoint 3 is unapproved; no approval commit, history rewrite or publication occurred. Original checkpoint header and unfinished deployment/test steps are unchecked. Focused non-checkpoint fix plan: `plans/in-progress/remove-standalone-daemons-keep-ui-mcp-20260908-cp04-v01.md`; this supplied filename repairs checkpoint 3. Checkpoint 4 remains untouched. No live service, shared-tool or global configuration changes were made.

Material fixes required
