# Checkpoint 2 v01 review — issue 37

Scope: safe plan admission through daemon, HTTP and MCP; pre-allocation rejection, reserved preparation identity, exact retry and redaction. Reviewed the uncommitted worktree against `c0afa1f` (approved cp1 v01), using the worktree fallback because checkpoint 2 has no commit boundary. Branch matches the original plan and its pre-handoff base is reachable. The worker artifact was read from the primary repository's `.aflow/runs/20260910t222324z-5514f100/turns/turn-003/result.json`; plan writes use this execution worktree.

## Findings

1. **P2, high confidence — Give malformed plans a correction that addresses their failure.** `aflow/api/startup.py:150–152` maps every non-recovery parse failure to the fixed Git Tracking message at lines 49–62. A plan containing exactly one valid Git Tracking section but no checkpoint headings receives “Add or correct exactly one Git Tracking section”; following that advice cannot make the plan runnable. The added malformed-plan HTTP test also asserts this incorrect correction. The same message hides the explicit started-base mismatch status at lines 600–605. This defeats the checkpoint's actionable-feedback requirement and removes useful source distinctions. A direct `_load_plan_with_recovery` reproduction confirmed the no-checkpoint case. Smallest fix: give the existing narrow typed boundary fixed source-classified corrections for known plan format and tracking/start-state failures, without parsing arbitrary exception strings. Preserve the selected safe correction and exact reserved ID on replay; do not suggest resetting started history.

2. **P2, high confidence — Keep unclassified preflight failures generic.** `aflow/daemon.py:944–959` converts all `OSError`, `ValueError` and `WorkflowError` failures in the preflight block to `plan_validation_failed`. The block includes backup I/O, repository probing, Git execution and atomic writes, so an unwritable backup directory for a valid pristine plan is reported as bad Git Tracking. A direct call with `_backup_original_plan` raising `PermissionError` confirmed that exact mapping with no run ID. This changes the existing generic contract and tells clients to edit a plan for an infrastructure failure, contrary to the explicit requirement to classify known source validation only. Smallest fix: annotate known validation rejections where identified, propagate only that typed subset, and retain opaque generic handling for unrelated failures. Apply the same discipline to new broad OSError/ValueError classification in startup helpers; do not expose raw causes.

## Verification and disposition

- `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 uv run pytest -q tests/test_control_plane_services.py tests/test_control_plane_resume.py tests/test_aflowd.py --tb=short`: 51 passed.
- `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 uv run --project apps/aflow_app/server pytest -q apps/aflow_app/server/tests/test_control_plane_api.py apps/aflow_app/server/tests/test_mcp.py apps/aflow_app/server/tests/test_auth.py`: 64 passed, three dependency deprecation warnings.
- `uv run ruff check aflow apps/aflow_app/server/src` and `git diff --check`: passed.
- Direct reproductions confirmed both findings; no production files were edited by this review.

Checkpoint 2 is rejected and unchecked; checkpoint 1 remains approved and checkpoint 3 pending. Repair plan: `plans/in-progress/issue-37-plan-heading-and-startup-errors-20260910-cp03-v01.md` (the supplied filename deliberately covers checkpoint 2). No approval commit, publication, history rewrite, or live changes. Physical keyboard remains unverified.

Material fixes required
