# Checkpoint 1 review — 2026-09-10

Target: active implementation of “Make hosted actions and CI interpreter selection deterministic,” branch `aflow-stabilize-hosted-browser-loading-and-ci-python-202-20260910-194923`.

Current-worktree fallback: no checkpoint commit exists for this plan. HEAD and reachable Pre-Handoff Base HEAD are both `be19c4ffffef80b4a53c8644a6908f8807557315`; six implementation files are dirty. Original and active plan are identical. Implementation checkmarks do not establish reviewer approval.

Reviewed CI interpreter selection, live-control readiness, navigation/scroll/refresh predicates, held restore response, production history action gating, component regression and DEVLOG. The supplied worker result `.aflow/runs/20260910t194922z-d0da40bf/turns/turn-001/result.json` is absent in this checkout; review used actual source and available documentation.

## Finding

**P2 · High confidence — Claimed WebKit verification still executes the history journey in Chromium.**

Location: `DEVLOG.md:35–37` (new evidence claim). Supporting launch sites: `apps/aflow_app/server/tests/test_run_navigation_browser.py:49` and `:277`.

The planned `AFLOW_TEST_BROWSER=webkit` command selects WebKit only in the responsive module. Both navigation tests explicitly launch `playwright.chromium`, including the newly changed held-response restore/delete regression. Thus the new “WebKit 13” claim is false and mandatory second-engine verification of the history repair is missing. This is a new completion/evidence defect, not a request to generally expand the old suite. It matters because this repair addresses browser-sensitive asynchronous ordering and explicitly requires both modules in both engines.

Smallest fix: make both navigation launch sites honor the existing validated Chromium/WebKit selection convention, preserve all assertions and the held restore response, run both planned engine commands without skips, and correct DEVLOG to record actual engines and results. No production redesign or dependency change is needed.

## Verification and disposition

Reviewer ran focused RunDashboard tests: 85 passed (React act warnings), and `git diff --check`: passed. Static inspection proves the engine mismatch without a timing-dependent reproduction. Worker-reported full web/build and browser results were not independently rerun during this rejected review and cannot establish WebKit navigation coverage.

Checkpoint 1 is not approved. Focused overlay: `plans/in-progress/stabilize-hosted-browser-loading-and-ci-python-20260910-cp01-v01.md`. Original checkpoint and verification step remain unchecked. No production edits, approval commit, history rewrite, publication, service changes or GitHub messages. Coordinator publication, exact-SHA CI and live desktop/mobile verification remain separate gates.

Material fixes required
