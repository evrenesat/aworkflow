# Checkpoint 3 review

Target: uncommitted worker turn 5 implementation after `52c7d89` (`cp2 v01`), on `codex/aflow-dogfood-20260909`. Worktree fallback: no CP3 worker commit exists. The bounded worker handoff from run `20260909t214357z-0efa55a8` identifies CP3; CP4 is the next unchecked checkpoint, not the reviewed scope. Prior approval commits and compatible implementation are retained.

Scope: budget exception evidence, executor prelaunch handling, diagnostic artifact persistence, reports, and Resume compatibility. No production code was edited during review.

## Finding

P2 — High confidence — Budget rejection still produces an incorrect root-cause report.

Location: `aflow/workflow.py:525–534`, where the new failure kind is attached to the fallback passed to the report renderer. Consumer: `aflow/manager.py:1213–1221` in `render_manager_stop_report`.

For a nonterminal manager boundary rejected by the byte guard, the fallback now identifies `manager_input_budget`, but the report renderer ignores that field and unconditionally writes “Manager output was unavailable, invalid, or illegal for the current controller boundary.” Terminal reports similarly characterize the absent response as an output failure. No provider ran. This violates CP3's explicit requirement not to describe input overflow as unavailable or invalid manager output and gives operators a contradictory root-cause diagnosis.

Evidence: a direct call to the production report renderer with the new budget-failure context and precise failure reason reproduces that root-cause text. The new runtime test checks the result reason and a turn reference but does not check report root cause.

Smallest correction: make the local fallback report recognize the explicit budget-failure kind and explain input-byte overflow before provider launch. Preserve terminal workflow incident evidence and legacy non-budget report behavior. Add focused report assertions to the existing budget-rejection regression.

## Verification

- Runtime manager suite: 42 passed, 7 subtests passed.
- Manager, manager context, runlog, and control-plane Resume suites: 159 passed.
- `git diff --check`: passed.
- Tests used disposable `Path.home()` configuration roots while respecting each test's own temporary HOME override. An initial fixed-home wrapper caused one live-skill-refresh test failure; correcting the wrapper made the full manager selection pass. This was not a product finding.

## Outcome

CP3 is not approved. Its failed step and checkpoint checkbox are reopened; other completed steps remain recorded. CP4–6 remain pending. Focused non-checkpoint fix plan: `plans/in-progress/aflow-manager-context-budget-followup-20260908-cp04-v01.md` (caller-specified destination despite the CP4 filename). No approval commit, history rewrite, or public push.

Material fixes required
