# CP3 repair review — not approved

Reviewed the immediately preceding worker's pending repaired CP3 worktree against approved CP2 `1bd1e2ad60b104fe4036f9b8065db6d49be06ea8`. No pending CP3 commit exists, so worktree fallback was used; older approved commits were not review targets. Scope: original CP3 live boundary reload, defaults/control precedence, retry prompt refresh, state persistence and terminal repair in the cp03-v02 overlay. CP4 remains outside scope.

1. **P2 — High confidence — Incomplete non-limit END becomes successful at the cap.** Location: `aflow/workflow.py:11045–11049` (consumed at `11349`). A live workflow with `max_turns = 1`, an unconditional `go = [{ to = "END" }]`, and a synthetic worker that leaves the original plan incomplete returns `status = "completed"`, `end_reason = "transition_end"`, and `final_snapshot.is_complete = False`. The new `limit_terminal` checks only the counter and END target, so it bypasses the incomplete-terminal failure even though the selected edge did not depend on the limit. This violates the active repair requirement to preserve non-limit incomplete END failures, and presents unfinished work as ordinary transition completion. Evidence: the temporary synthetic reproduction returned exactly `completed transition_end False`; `_normalize_end_reason` preserves unconditional END as `transition_end`. Smallest correction: permit the incomplete-END exception only when the selected condition actually requires MAX_TURNS_REACHED to match; preserve normal pre-turn limit exhaustion and supported capped conditional END. Add a synthetic regression for unconditional END at the cap and retain ordinary incomplete-END failure before the cap.

The earlier lowered-live-limit and conditional capped-END failures are repaired. Cumulative persistent choices remain intact. No other candidate survived the material finding admission gate.

Verification: required runtime/state/runlog suite — 339 passed, 43 subtests; focused CLI resume override suite — 2 passed, 8 subtests; git diff --check passed. Tests and reproduction used synthetic providers and temporary fixtures. Manager code is unchanged: retain the 40 KiB hard guard and 16 KiB compact summary.

CP3 remains unapproved. No approval commit or history rewrite. Compatible implementation and existing implementation markers are retained. New non-checkpoint repair overlay: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp03-v03.md`; superseded CP3 cp03-v02 is removed. The cp03-v01 overlay belongs to CP2 and is retained. Neither CP4 nor whole-plan completion advances.

Material fixes required
