# CP2 repair review — 2026-09-10

Reviewed active non-checkpoint overlay cp02-v01 against original CP2, including
the current accumulated CP2 implementation and App intent repair. No CP2
commit boundary exists, so current-worktree fallback from approved cp1 v01
(`4bf09ae8a1847d7ea29e3230e2a6d50a84a01b5c`) was used. Branch matches the
original plan and its pre-handoff base remains reachable and unchanged.
Scope: shared compact list/detail presentation, Settings and Runs consumers,
navigation intent, related tests and documentation. CP3–6 were not reviewed.

## Finding

**P2 — High confidence — All runs selection still leaves compact detail hidden.**
Location: `apps/aflow_app/web/src/App.tsx:611–613`; callback at line 519.
The new explicitRunNavigation prop requires runNavigationIntent, but All runs
onOpen only calls applyQuery. Starting at All runs on a phone and selecting a
run reaches its exact URL while displaying history with detail hidden. Users
must locate and select the same run again. The requested cp02-v01 correction
and its fresh/repeat All runs tests are absent from current code.

Evidence: a fresh production build and disposable Chromium at 390×844 reproduced
`?view=all-runs` → select review-run →
`?project=test-project&view=runs&run=review-run`, history_visible=True,
detail_visible=False. The visible-detail assertion failed after two seconds.
Diagnostic: `/tmp/test_cp2_review_entry.py`, using repository disposable fixtures
and an ephemeral localhost server. No production records or workflow launches.

Smallest proportionate fix: supply the existing App-owned navigation intent
from All runs onOpen alongside the exact query transition; retain passive
selected-run synchronization as list-first. Cover fresh and same-run repeat
entry after local Back with App and real-browser regressions. No new router,
state store, request/subscription logic or domain redesign is required.

## Verification and disposition

- Local verification: 177 targeted web tests passed; production build passed;
  existing Chromium modules passed all four tests. The additional All runs
  reproduction failed as described above. `git diff --check` passed.
- CP2 remains unapproved; Last Approved Checkpoint remains cp1 v01. CP3–6
  remain unchecked. No approval commit or history rewrite occurred.
- Replaced the superseded cp02-v01 overlay with
  `plans/in-progress/responsive-document-scroll-and-mobile-editing-20260910-cp02-v02.md`.
  Updated original review log without altering checkpoint authority or base.
  Rotated the previous review byte-for-byte. Implementation and DEVLOG preserved.
- Publication: not performed. Exact-SHA CI: not run. Live activation/usability:
  not verified. Normal workflow owns delivery after plan completion.

Material fixes required
