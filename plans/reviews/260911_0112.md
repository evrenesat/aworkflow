# Cumulative review — issue 35 repair progress

Original/active plan: `plans/in-progress/issue-35-repair-progress-20260909.md`.
Branch: `aflow-issue-35-repair-progress-20260909-20260910-230639`.
Unchanged Pre-Handoff Base HEAD: `a268c78b9e1dcdbebf3125c17669d42810f29414`.
Reviewed head: `1b9aa1d3ebd11e01df9981866c16b5d4869400e6`.
New commits: 4. Total handoff commits: 4. Coverage: all original requirements and actual accumulated changes in `cp1 v01` (`a01bd4d4`), `cp2 v01` (`55c0d9bb`), `cp3 v01` (`7f17ff93`), and `cp4 v01` (`1b9aa1d3`).

Previous findings: none for this handoff. Prior latest review concerned a different plan and was rotated byte-identically to `plans/reviews/260911_0053.md`. The supplied worker artifact `.aflow/runs/20260910t230638z-89fc9ad0/turns/turn-004/result.json` is absent from this execution root; verification was independently performed against committed code. All original checkpoints are checked. Initial and post-test tracked worktree status were clean.

## R1 — Preserve pre-change historical repair-context reconstruction

Severity: P2. Confidence: high. Location: `aflow/manager_context.py:885–928`, with unconditional authority selection at `1994–2015`. Introduced by `1b9aa1d3` (`cp4 v01`).

For a valid selector-4/schema-v3 repair context captured before this handoff, the active unheaded overlay produced unavailable checkpoint evidence. Rebuilding its durable boundary now retains `captured_plan_state` but promotes original-scope checkpoint evidence to available and changes `plan_content_disclosure`. Existing `analyze_runs` rejects this with `ValueError: manager context drift for decision-001 (evidence, plan_content_disclosure)`. This breaks historical analysis of existing supported runs and violates the explicit historical-payload preservation contract.

Evidence: `.aflow/review-issue35/reproduce_history_drift.py` loads the original base module in memory, constructs a pre-change repair context with valid v2 envelope and schema-v3 history metadata, writes disposable durable artifacts, and invokes current `analyze_runs`. Independently reproduced the exact drift error; stored artifact bytes remained unchanged. Capturing and rebuilding only with the new builder, as the added history test does, misses this upgrade regression.

Smallest coherent fix: retain legacy evidence/disclosure construction for pre-change captured boundaries while explicitly distinguishing new live original-authority captures and their rebuilds through the existing durable boundary. Preserve drift detection and all scope/digest/containment checks; do not modify historical artifacts. Add a pre-change fixture through actual historical analysis plus continued new-context reconstruction coverage.

## Verification evidence

- `uv run pytest -q tests/test_run_progress.py tests/test_control_plane_repository.py tests/test_control_plane_services.py tests/test_manager_context.py tests/test_manager.py tests/test_runtime.py tests/test_live_config_runtime.py`: 467 passed, 43 subtests passed, 2 failures. Both failures were existing lifecycle fixture commits rejected by the host global `plans` ignore rule.
- Process-isolated rerun: `GIT_CONFIG_GLOBAL=/dev/null uv run pytest -q tests/test_runtime.py::WorkflowLifecycleRuntimeTests::test_rejected_final_lifecycle_push_resumes_without_replaying_worker tests/test_runtime.py::WorkflowLifecycleRuntimeTests::test_two_tracked_plan_runs_publish_ignored_done_lifecycle_cleanly --tb=short`: 2 passed. No global configuration changed.
- `uv run --project apps/aflow_app/server pytest -q apps/aflow_app/server/tests/test_mcp.py apps/aflow_app/server/tests/test_control_plane_api.py`: 45 passed.
- `npm --prefix apps/aflow_app/web test -- --run`: 336 passed across 22 files. React act warnings were non-failing.
- `npm --prefix apps/aflow_app/web run build`: passed, current bundle built before browser checks.
- `AFLOW_TEST_BROWSER=chromium uv run --project apps/aflow_app/server pytest -q apps/aflow_app/server/tests/test_run_progress_browser.py`: 2 passed.
- `AFLOW_TEST_BROWSER=webkit uv run --project apps/aflow_app/server pytest -q apps/aflow_app/server/tests/test_run_progress_browser.py`: 2 passed.
- Both browser commands additionally used execution-local `AFLOW_BROWSER_ARTIFACT_DIR`; twelve screenshots are retained under `.aflow/review-issue35/{chromium,webkit}/`. Visually inspected Chromium 390px repair and WebKit desktop next-checkpoint screenshots. Physical mobile keyboard behavior remains unverified.
- `uv run ruff check aflow` and `git diff --check`: passed.
- `uv run python .aflow/review-issue35/reproduce_history_drift.py`: confirmed R1 through actual `analyze_runs`; all historical output lines prefixed `HISTORY:`.

A proposed stale-primary-plan concern was excluded after confirming `workflow.py` synchronizes execution-plan bytes to primary before scope closure. No other finding met the admission gate. Review remains cumulative; passing tests do not override demonstrated R1.

Rejected without squash or history rewrite. Exactly one focused non-checkpoint fix plan created at `plans/in-progress/issue-35-repair-progress-20260909-cp01-v01.md`. No older fix plans for this handoff existed. Original plan remains in place for engine lifecycle management; DEVLOG was not compacted. Publication, exact-SHA CI, live activation and ticket acceptance remain coordinator-owned and are not claimed here.

Material fixes required
