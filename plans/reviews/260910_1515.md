# CP5 attempt v01 review

Reviewed the immediately preceding uncommitted CP5 attempt against original
`plans/in-progress/responsive-document-scroll-and-mobile-editing-20260910.md`.
Current-worktree fallback from approved cp4 v02 (`12c9af1`) was required because
no CP5 commit exists. Branch matches; pre-handoff base is reachable. CP6 is excluded.
Covered remaining responsive journeys, launch tables, Plans Back, App alert focus,
and browser regressions. Applied material-code-review admission gate and exclusions.

## Finding

P2 — High confidence — `apps/aflow_app/web/src/styles.css:1612–1617`.
Compact launch-table captions retain native table-caption layout after their table
becomes a block. Opening New Run with the disposable managed workflow at320×568
produces246px tables but67.375px/59.375px captions,166px/94px tall. Captions wrap
almost word per line and waste180px of phone task space before downstream launch
content. Inspected `/tmp/cp5-launch-review.png` and measured Chromium geometry.
A browser-only block/full-width caption rule gives246px widths and40px heights
for both. Smallest fix: apply that rule only in the existing compact media query,
with a caption-width browser regression assertion; retain desktop table layout.

## Verification and verdict

Full web suite297 passed; production build passed; existing two Chromium modules
5 passed. Additional disposable Plans/New Run probe passed overflow checks at
320×568,844×390,1280×720,1440×900 and desktop112px header checks. Its caption
measurements and screenshot exposed the finding despite those passing checks.
`git diff --check` passed. WebKit/full acceptance matrix remains CP6 work.

CP5 remains unapproved; CP6 remains unchecked. Focused non-checkpoint repair:
`plans/in-progress/responsive-document-scroll-and-mobile-editing-20260910-cp06-v01.md`.
No older CP5 fix overlay existed; prior checkpoints' overlays are preserved.
No production code edits, approval commit, history rewrite or shared runtime changes.
Publication: pending. Exact-SHA CI: pending. Live activation/proof: pending.

Material fixes required
