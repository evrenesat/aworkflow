# CP5 review — material fixes required

Reviewed the immediately preceding worker's pending original CP5 implementation through the current worktree against approved CP4 `62d8ac91c870d8a939dbc6bc4f65e40b7446aad4`. Worktree fallback: no pending CP5 commit exists; zero intervening implementation commits. Scope: shared live control admission, capability loading, HTTP/MCP validation mapping, boundary validator integration and tests. CP6 is outside scope.

- **P2 · High confidence — Validate the effective pending next step.** `aflow/control_plane/services.py:148-151` selects the old status step while ignoring `existing.next_step`. A supported pending correction to `implement` after removing the old step is preserved by CAS and honored by runtime, yet submitting a valid team update is rejected against the removed old step. Synthetic temporary-run reproduction returned `ControlValidationError: current_step 'removed_step' is not an executable step in workflow 'managed'`; override bytes remained unchanged. This blocks valid recovery controls. Resolve the pending next_step before status fallbacks and test both valid correction acceptance and invalid pending-step rejection. Keep the fix local to admission and preserve replay/CAS/owner-stop semantics.

Verification: required service/capability/state/API/MCP plus runtime/live-config/hotplug suites: 421 passed, 43 subtests; git diff --check passed. No real providers or operational changes. Manager 40 KiB hard guard and 16 KiB compact summary retained.

CP5 remains unapproved; compatible implementation and its step markers retained. Original plan review log updated; CP6 remains unchecked. Focused non-checkpoint repair: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp06-v01.md` (targets original CP5). No history rewrite or approval commit. Earlier overlays target other checkpoints and are not superseded by this repair.

Material fixes required
