# Checkpoint 2 review — not approved

Reviewed checkpoint 2 initial attempt using the current-worktree fallback after
approved cp1 v01, HEAD e7253ac3a401dc09c8373d0b058444d0c6323277, on branch
aflow-automated-changelog-and-delivery-20260909-20260910-184255. There is no cp2
commit boundary. Original and active plan are both
plans/in-progress/automated-changelog-and-delivery-20260909.md. Scope: Settings
Changelog rendering, paging, draft retention, header integration, browser tests
and associated docs/build hook. Base 1153d35 remains reachable; branch matches.
The supplied turn-007 result artifact is absent at its worktree-relative path.
Review relies on current source and fresh verification. CP1 is not re-reviewed.

## Finding

**P2 — Hide editing actions on the read-only Changelog surface.** High confidence.
Location: apps/aflow_app/web/src/components/GlobalSettings.tsx:551–558;
the fallback repeats Save at line 569 and the new Changelog branch at line 576
makes these unconditional actions reachable on the release view.

Edit General → Bind host, open Changelog, click Save all changes. The enabled
button invokes the shared save() and submits PUT /api/settings with the hidden
General draft. A real Chromium probe intercepted the request and confirmed
bind_host=unsaved-review.example; no persistence was performed by the probe.
The original plan expressly requires Changelog never to enable Save/write
settings and requires hiding irrelevant editing controls. Users can currently
persist invisible edits from this promised read-only surface.

Smallest correction: condition hosted and fallback editing actions on the
actual displayed surface being editable, retaining header slots, navigation,
the unsaved indicator and the single parent draft/save owner. Returning to
General/Skills must restore Save and the exact retained draft. Advanced TOML
remains editable even when the stored tab value is Changelog. Add a focused
unit/browser regression for this demonstrated action-boundary failure.

## Verification

- Standard web suite: 19 suites / 320 tests pass.
- Production build passes; git diff --check passes.
- Chromium and WebKit each collect and pass the real Changelog journey, with
  seven required viewports and both themes, no skipped journey.
- Inspected fresh Chromium light 320×568/dark 1280×720 and WebKit dark 390×420
  screenshots. Draft round trips, paging and document movement pass; screenshot
  evidence also shows the active Save action on Changelog.
- Isolated Chromium interception probe passes and demonstrates the finding:
  /tmp/aflow-changelog-cp2-review-probe-20260910/test_readonly.py.
- Browser screenshots: /tmp/aflow-changelog-cp2-review-20260910-chromium/ and
  /tmp/aflow-changelog-cp2-review-20260910-webkit/.
- Reuse CP1's approved generator/cache/packaging proof. Final CP2 wheel/login
  verification remains before approval; this rejection does not claim it.
  Physical keyboard remains unverified.

## Decision and bookkeeping

Checkpoint 2 remains unapproved; its heading is unchecked while implementation
substeps remain recorded. Focused non-checkpoint repair plan:
plans/in-progress/automated-changelog-and-delivery-20260909-cp01-v02.md (supplied
filename, checkpoint 2 scope). Existing cp01-v01/cp02-v01 overlays are retained
under the explicit owner instruction to preserve durable references. Original
plan review log updated; Pre-Handoff Base HEAD unchanged. No approval commit,
production-code edit, DEVLOG compaction, squash or history rewrite occurred.
Publication, exact-SHA CI, live activation and lifecycle bookkeeping remain
coordinator-owned and pending.

Material fixes required
