# Pending CP2 review

Reviewed the immediately preceding worker's CP2 launch/resume changes against approved CP1 `7454db7`, using the current worktree fallback because there is no pending CP2 commit. Older checkpoint commits belong to preceding plans. CP3 is future work, not this review target. Covered current-source startup/resume, daemon admission, provenance and metadata serialization. No implementation or approval commit was created.

1. **P2, high confidence — aflow/runlog.py:1310–1315.** The writer unconditionally reads `start_step_explicit`, but `ControllerState` does not declare that field. Normally constructed state causes `AttributeError` instead of persisting status or finalizing failure. Existing tests `test_atomic_run_metadata_failure_preserves_previous_snapshot` and `test_workflow_failure_finalizer_persists_stops_banner_and_chains_cause` reproduce this regression. Smallest fix: declare `start_step_explicit: bool | None = None` alongside the other ControllerState provenance fields; retain writer validation.

2. **P2, high confidence — aflow/cli.py:1386–1387.** An explicit resume step is ignored when it equals the original starting step. A run starts at `implement_plan`, later fails environment preflight at `removed_review`, and current settings remove that review step. `--start-step implement_plan` produces `start_step_override=False` and retains `interrupted_step_name=removed_review`, so the controller selects the removed target despite the valid correction. A temporary synthetic bootstrap fixture reproduced those values. Smallest fix: track explicit supplied-step intent independently of equality to the original starting step; preserve omitted-argument behavior and pending finalized-turn evidence.

Required CP2 suite: 251 passed, 125 subtests passed. Additional library/API/runtime suite: 309 passed, 49 subtests passed, 3 failed. Two failures establish finding 1; the remaining `test_resume_identity_drift_fails_before_event_or_new_run_state` expects the intentionally removed frozen-config gate and belongs to the planned later restriction-test cleanup, not an additional material finding. Synthetic bootstrap reproduction confirms finding 2. `git diff --check` passed. No real providers or production run state were used.

CP2 remains unapproved; its implementation is preserved. Repair overlay: `plans/in-progress/live-configuration-without-snapshot-gates-20260909-cp03-v01.md` (CP2 scope despite the supplied filename). Review original CP2 after repair.

Material fixes required
