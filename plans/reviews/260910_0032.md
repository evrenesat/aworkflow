# CP6 repair review — 2026-09-10

Reviewed CP6 worker turn 9 of run `20260909t224505z-752d79ec`, identified by its result and bounded handoff, against original `plans/in-progress/aflow-manager-context-budget-followup-20260908.md` and active CP6 overlay `aflow-manager-context-budget-followup-20260908-cp01-v01.md`. Used current-worktree fallback after CP5 approval `754331bfbfd1937ba1452c9c9e0d42ef19edd386`; no CP6 worker commit exists. Branch matches and original pre-handoff base remains reachable. Scope is CP6 readable labels, collision repair, raw identifier integrity, affected tests and documentation; earlier checkpoints were not re-reviewed.

## Finding

**P2 — Disambiguate role-assignment control labels. High confidence.**

Exact locations: `apps/aflow_app/web/src/components/GlobalSettings.tsx:546` and `:567`; the same root cause occurs in `apps/aflow_app/web/src/components/RunDashboard.tsx:1893–1896`.

Supported distinct roles `code_review` and `code__review` collapse to identical visible and accessible labels. When both initially use the same profile, Global roles shows two indistinguishable “Role Code review” controls and Teams shows two “Code review” controls. Selecting a profile changes a different raw role in each, so users can save an unintended assignment. Before CP6, the control labels exposed the distinct raw roles.

Evidence: `_parse_role_map` in `aflow/config.py:403–414` accepts both role keys. A disposable component reproduction passed assertions for both duplicate control pairs and the absence of raw role text in the global editor. The callbacks still target their separate raw keys. Live role-selector controls likewise apply `formatMachineLabel(role)` independently to each role. This meets the material finding gate: reachable supported input, introduced by this checkpoint, concrete wrong-assignment risk, and a local correction.

Smallest fix: reuse `formatMachineChoice` for global role labels, the selected team's union of role labels, and live controls' visible/accessibility labels using their actual sibling collections. Preserve exact role keys and profile values; add focused selection/save regressions. No redesign or unrelated read-only-label audit is required.

## Verification and disposition

- 277 web tests, production build, four disposable Chromium checks and diff check passed. The one-test disposable role-collision reproduction confirmed the defect and was removed.
- Completed team/workflow/native-option repairs remain compatible and retained. CP6 remains unapproved and only its collision step was reopened.
- Replacement focused plan: `plans/in-progress/aflow-manager-context-budget-followup-20260908-cp06-v01.md`. Superseded CP6 cp01-v01 overlay removed; unrelated completed CP3 cp04-v01 overlay preserved.
- Prior CP1–CP5 approval commits and lineage remain intact. No approval commit, history rewrite, production code edit, live service/global configuration/installed-skill mutation or public push occurred during review.

Material fixes required
