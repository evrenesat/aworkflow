# Integration checkpoint 1 review — 2026-09-10

CP1 v01 is **not approved**. Review used the current-worktree fallback against `52597409b17c3e1050f1576bfe027580b5656d50`: no integration checkpoint commit exists. Branch `aflow-integrate-reviewed-responsive-ui-20260910-20260910-161156` matches the original plan; its pre-handoff base is reachable. Pending `MERGE_HEAD` remains `61d5988f98584ffa50ce0a6e02a0f5404a423276`. No commit, history rewrite, publication, external message, live setting/service/shared-tool change, or other-worktree mutation occurred.

Original and active plan: `plans/in-progress/integrate-reviewed-responsive-ui-20260910.md`. Review targets the immediately preceding worker's reconciliation of approved responsive source with accepted main, including conflict resolutions, automatically merged App/RunDashboard behavior and combined acceptance. The original six approvals are preserved. Production conflict resolutions retain both the responsive shell and main's preflight/live configuration controls; no material production defect was established.

HISTORY: The worker turn-001 result was found read-only at `/root/code/agent_flow/.aflow/runs/20260910t161156z-5651f38c/turns/turn-001/result.json` in the registered parent root. The supplied execution-root-relative path was absent.

## Finding

### P2 — Wait for loaded plan rows before counting them

- Confidence: high.
- Location: `apps/aflow_app/server/tests/test_responsive_browser.py:321-322`.
- Scenario: navigating to Plans displays the New plan filename input while `PlanPanel` is still awaiting `listProjectPlans`. The test waits only for that input, then synchronously counts list buttons. The review's Chromium tablet journey observed zero rows and failed `0 >= 40`.
- Impact: the newly integrated required browser suite can fail on ordinary asynchronous loading, blocking combined acceptance and the CI delivery gate without a product failure.
- Evidence: `/tmp/integration-review-chromium.log`, `test_responsive_route_matrix[tablet]`; 12 passed, 1 failed. `PlanPanel.tsx:48` initializes `plans=[]`, loads it asynchronously at lines 76–90, and contributes the filename input independently at line 230. The full backend invocation passed separately; that does not invalidate this failure or establish a correction.
- Smallest coherent fix: wait for the expected loaded fixture plan rows, then retain the existing >=40 count, geometry, document movement and action assertions. Verify delayed list loading rather than adding a sleep, increasing timeouts, weakening the count or rerunning until green.

HISTORY: The assertion is inherited unchanged from reviewed source `61d5988`; it is newly added relative to the integration's accepted-main parent. This is a concrete failed combined-test requirement within the companion's acceptance scope, not a claim that the worker invented a new production regression or that the original six approvals are revoked.

## Additional checkpoint acceptance gap

The original CP1 step requiring live team/selector/max-turn changes and restart/successor actions inside the responsive shell is not evidenced. `test_responsive_browser.py:43-51` seeds completed legacy runs; the run journey at lines 338–354 only opens their detail. The inspected fresh Chromium dark run-detail screenshot explicitly reports that workflow controls are unavailable. No browser journey operates live control saves/rejections or restart/successor requests. Component tests and the real dirty-preflight ordinary-launch check are useful but do not prove these controls remain usable through resize, Back/navigation, errors and exact-request retry in the hosted shell.

This is an explicit plan acceptance gap, not a speculative second code finding. Supply disposable Chromium/WebKit journeys and loaded screenshots under the focused overlay; avoid redesign and unrelated coverage expansion.

## Verification

- Web: 309 passed; production build passed.
- Full backend: 2043 passed and 223 subtests passed, no skips reported.
- Separate Chromium combined suite: 12 passed, 1 failed (tablet plan-list readiness above), no retry.
- WebKit responsive suite: 8 passed, no skips reported.
- Ruff and staged/unstaged `git diff --check`: passed; no unresolved index entries.
- Inspected fresh WebKit light Skills editing and dark landscape New Run screenshots, plus Chromium dark run detail. Skills shows usable editor space; New Run shows a loaded dirty preflight; run detail confirms the live-control evidence gap. This is not a claim of complete screenshot acceptance.
- Logs: `/tmp/integration-review-{web,build,backend,chromium,webkit}.log`. Fresh images are under `/tmp/integration-review-chromium/` and `/tmp/integration-review-webkit/`.
- Physical mobile keyboard/browser-toolbar behavior remains unverified.

## Bookkeeping and delivery

Original CP1 and its combined-control/browser acceptance steps are unchecked. Focused non-checkpoint fix plan: `plans/in-progress/integrate-reviewed-responsive-ui-20260910-cp01-v01.md`. No older same-checkpoint overlays existed to supersede. Original pre-handoff base, original responsive plan and all prior approvals remain unchanged; DEVLOG was not compacted. Reviewer changes are limited to plan/review artifacts.

The existing `origin/main` reference includes accepted clipboard repair `c14f1f2` through `cd78d53`; the pending integration does not yet contain it. Subsequent reviewer/coordinator delivery must retain that accepted history, without duplicating the repair or modifying its separate root. The current failed review creates no approval commit or push. Publication, exact-SHA CI, and live desktop/mobile activation remain pending downstream gates.

Material fixes required
